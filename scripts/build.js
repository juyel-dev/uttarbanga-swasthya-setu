// Simple build: this is a zero-build vanilla project (ES modules served directly).
// For production, consider esbuild/rollup to bundle src/js + src/components + src/pages.
console.log('No build step required. Deploy the folder as-is.');
